const LCM = require("./question-25");

test("Find LCM of two numbers ", () => {
  expect(LCM(12, 18)).toBe(36);
});

test("Find LCM of two numbers ", () => {
  expect(LCM(5, 7)).toBe(35);
});

test("Find LCM of two numbers ", () => {
  expect(LCM(11, 8)).toBe(88);
});

test("Find LCM of two numbers ", () => {
  expect(LCM(14, 6)).toBe(42);
});

test("Find LCM of two numbers ", () => {
  expect(LCM(4, 13)).toBe(52);
});
